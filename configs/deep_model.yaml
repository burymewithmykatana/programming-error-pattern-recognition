random_seed: 42
model:
  name: microsoft/codebert-base
  max_length: 256
  num_labels: 8
training:
  epochs: 3
  batch_size: 8
  learning_rate: 0.00002
notes: "Placeholder config for future Hugging Face transformer fine-tuning."

